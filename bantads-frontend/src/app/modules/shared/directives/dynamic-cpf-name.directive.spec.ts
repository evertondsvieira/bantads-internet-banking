import { DynamicCpfNameDirective } from './dynamic-cpf-name.directive';

describe('DynamicCpfNameDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicCpfNameDirective();
    expect(directive).toBeTruthy();
  });
});
