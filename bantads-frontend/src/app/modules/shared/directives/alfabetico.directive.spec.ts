import { AlfabeticoDirective } from './alfabetico.directive';

describe('AlfabeticoDirective', () => {
  it('should create an instance', () => {
    const directive = new AlfabeticoDirective();
    expect(directive).toBeTruthy();
  });
});
