import { AlfanumericoDirective } from './alfanumerico.directive';

describe('AlfanumericoDirective', () => {
  it('should create an instance', () => {
    const directive = new AlfanumericoDirective();
    expect(directive).toBeTruthy();
  });
});
